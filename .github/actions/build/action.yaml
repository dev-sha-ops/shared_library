name: "Build Action"
description: "A reusable custom action."
inputs:
  tags:
    description: "Provide Github Sha as Tag"
    required: true
runs:
  using: "node23" # or "docker" depending on your setup
  main: "index.js"
